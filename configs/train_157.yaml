experiment_name: experiment_name
checkpoint_dir: checkpoint_dir

dataset_dir: dataset_dir

model_name: llama_unconditional_gated_attention

num_steps: 50000
batch_size: batch_size
sequence_length: 4096
context_length: null

num_warmup_steps: 2000
min_lr_ratio: 0.1

lr: 3.0e-4
betas: [0.9, 0.95]
weight_decay: 0.1
max_grad_norm: 1.0

model_config:
  vocab_size: 32000
  hidden_size: 3072
  num_layers: 32
  num_heads: 24
  num_kv_heads: 24
  head_dim: 128
  intermediate_size: 8256
  norm_eps: 1.0e-05
  rope_theta: 10000.0
  max_sequence_length: 4096
  gated_attention_type: standard
